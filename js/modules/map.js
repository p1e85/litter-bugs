// This module contains all logic directly related to the Mapbox GL JS library.
import * as state from './state.js';
import { createPinPopup } from './ui.js';
// ... (other necessary imports)

export function initializeMap(container) {
    // ... (logic to create new mapboxgl.Map)
}

export function initializeMapLayers() {
    // ...
}
// ... (all other map-related functions: changeMapStyle, toggleMarkerVisibility, findMe, etc.)
